apiVersion: v1
kind: Service
metadata:
  name: employee-api-service
  namespace: employee
spec:
  selector:
    app: employee-api
  type:  NodePort
  ports:
  - name: http
    protocol: TCP
    port: 4000
    targetPort: 4000